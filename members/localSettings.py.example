import ldap
from django_auth_ldap.config import LDAPSearch

""" LDAP Configuration """

AUTH_LDAP_SERVER_URI = "ldap://ldap.mycompany.org"

AUTH_LDAP_BIND_DN = "cn=binduser,dc=mycompany,dc=org"

AUTH_LDAP_BIND_PASSWORD = "mypassword"

AUTH_LDAP_USER_SEARCH = LDAPSearch("ou=users,dc=mycompany,dc=org",
    ldap.SCOPE_SUBTREE, "(uid=%(user)s)")

AUTH_LDAP_USER_ATTR_MAP = {"first_name": "givenName", "last_name": "sn", "email": "Email"}

AUTH_LDAP_USER_FLAGS_BY_GROUP = {
    "is_active": "cn=active,ou=groups,dc=example,dc=com",
    "is_staff": "cn=admin,ou=groups,dc=example,dc=com",
    "is_superuser": "cn=superuser,ou=groups,dc=example,dc=com"
}


